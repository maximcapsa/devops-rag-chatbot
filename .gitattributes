# Normalize line endings to LF in the repo (avoids CRLF breaking shell scripts).
* text=auto eol=lf

# Scripts that MUST be LF (executed on Linux / inside containers).
*.sh        text eol=lf
*.tftpl     text eol=lf
Dockerfile  text eol=lf
Makefile    text eol=lf
